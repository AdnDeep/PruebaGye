@{
    string username = ViewData["username"] == null ? "Usuario" : (string)ViewData["username"];
}
<header id="header" class="header">

    <div class="header-menu">

        <div class="col-md-7">
            <a id="menuToggle" class="menutoggle pull-left">
                <i class="fa fa fa-bars"></i>
            </a>
        </div>
        <div class="col-md-5">
            <div class="user-area dropdown float-right">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    @username<i class="menu-icon fa fa-chevron-down"></i>
                </a>
                <div class="user-menu dropdown-menu">
                    <a class="nav-link" asp-controller="Home" asp-action="Logout"><i class="fa fa-sign-out textColorMenu"></i> Cerrar Sesi&oacute;n</a>
                    @*<a class="nav-link" href="@Url.Action("Logout", "Home")"><i class="fa fa-sign-out textColorMenu"></i> Cerrar Sesión</a>*@
                </div>
            </div>
        </div>
        @*<div class="col-md-5">
                <div class="user-area float-right">
                    <a class="nav-link" asp-controller="Home" asp-action="Logout"><i class="fa fa-sign-out textColorMenu"></i> Cerrar Sesi&oacute;n</a>
                </div>
            </div>*@
    </div>
</header>